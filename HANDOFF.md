# 🛡 HANDOFF — neuro-site

> Живий файл передачі справ. Угорі — актуальний стан. Нижче — журнал змін караула (нове зверху).
> Останнє оновлення: 2026-07-01 · Зміну здав: claude/web-session (гілка claude/project-file-access-oxsc3m) · Коміт: 89147ae

## 🟢 Поточний стан
`neuro-site` — вебзастосунок на **Next.js** (App Router, Turbopack, TypeScript, Tailwind, Biome, bun як пакетний менеджер). Робота цієї сесії стосувалася **не застосунку**, а інфраструктури: створено скіл `shift-handoff` («зміна караула») у `.claude/skills/`.
Гілка: `claude/project-file-access-oxsc3m` · Останній коміт: `89147ae "Rewrite shift-handoff skill in English per skill conventions"` · Робоче дерево: чисте · Синхронізовано з origin: так · Складання: НЕ запускалось · Тести: у проєкті не виявлено тестового скрипта.

## 🎯 Активні плани / незроблене
- **[Користувач перевіряє]** Скіл `shift-handoff` треба протестувати в **іншому чаті / іншому проєкті** — переконатися, що тригер («зміна караула», «передай пост») спрацьовує й `HANDOFF.md` генерується. Наступний крок: користувач відкриває нову сесію й дає команду.
- **[Дія користувача]** Встановити скіл в акаунт кнопкою **«Save skill»** на надісланому `.skill`-файлі, щоб він був у кожному проєкті на десктопі й у майбутніх сесіях. З цієї пісочниці я цього зробити не можу (немає доступу за межі контейнера).

## 🕳 Пройоби, борги та знахідки
- **Пройоб (виправлено):** першу версію `SKILL.md` я написав повністю українською — це порушує конвенцію skill-creator (name/description/тіло мають бути англійською). Переписано англійською в коміті `89147ae`, тригерні фрази лишились українськими, вміст `HANDOFF.md` генерується мовою користувача.
- **Знахідка:** глобальні копії скіла, які я поклав у `~/.claude/skills/` цього контейнера, **ефемерні** — зникнуть після завершення сесії. Єдиний довговічний шлях на машину користувача — кнопка «Save skill» або локальний Claude Code. Це архітектурна межа хмарної сесії, а не помилка.
- **Тех-борг:** `lint`-скрипт проєкту — це `tsc --noEmit && next lint`; повний type-check/збірку в цій сесії не проганяли.

## ✅ Перевірка (що і як перевірено)
- Перевірено: стан git — `git log`, `git status` (робоче дерево чисте, у синхроні з origin), коміти `b0eabdb` і `89147ae` запушені.
- Перевірено: скіл присутній і завантажується — `shift-handoff` з'явився у списку доступних скілів і був викликаний у цій сесії (цей файл — результат його роботи).
- Перевірено: скіл валідний — `package_skill.py` вивів «✅ Skill is valid!» при пакуванні.
- НЕ перевірено: складання/type-check (`next build`, `tsc --noEmit`) — причина: не входило в задачу сесії, запуск довгий; стан застосунку не змінювався.
- НЕ перевірено: фактичне спрацювання тригера в **іншому** чаті/проєкті — причина: це наступний крок користувача, у цій сесії скіл викликано вручну.

---

## 🗂 Журнал змін караула

### 🔁 2026-07-01 — зміну здав claude/web-session
- **Зроблено з минулої зміни (перша зміна; база — коміт `262613d`, останній перед роботою сесії):**
  - Створено скіл `shift-handoff` — `.claude/skills/shift-handoff/SKILL.md` (коміт `b0eabdb`).
  - Переписано `SKILL.md` англійською за конвенцією skill-creator; тригери лишено українськими (коміт `89147ae`).
  - Запаковано у `shift-handoff.skill` і надіслано користувачу; встановлено глобальні копії в `~/.claude/skills/` контейнера.
- **Стан на момент передачі:** гілка `claude/project-file-access-oxsc3m`, робоче дерево чисте, усе запушено в origin. Код застосунку не чіпався.
- **Не зроблено / відкладено:** встановлення скіла в акаунт користувача (кнопка «Save skill» — дія користувача); перевірка тригера в іншому чаті (наступний крок користувача).
- **Пройоби та знахідки:** перша версія скіла була українською — виправлено на англійську. Глобальні копії в контейнері ефемерні — довговічний шлях лише через «Save skill» або локальний Claude Code.
- **Перевірено / не перевірено:** git-стан, наявність і валідність скіла, його виклик — перевірено особисто. Складання застосунку й спрацювання тригера в іншій сесії — НЕ перевірено (поза межами задачі).

<!-- ↑ нові записи додавай сюди, старі не видаляй ↓ -->
