# 📘 ТЕХНІЧНИЙ ПАСПОРТ ПРОЄКТУ "NEURO-SITE"

## 1. Огляд системи
Цей проєкт — автономний інтернет-магазин (SPA), який працює без класичного бекенду та бази даних.
* **Frontend:** Next.js 15 + Tailwind CSS.
* **Хостинг:** Netlify (Free Plan).
* **CMS (Адмінка):** `/admin` (авторська розробка, працює через GitHub API).
* **База даних:** Файл `src/lib/courses-data.ts` у репозиторії.
* **Файлове сховище:** Папка `public/images` у репозиторії.
* **Оплата:** WayForPay (підпис генерується через Serverless Function).

---

## 2. Критичні налаштування (Environment Variables)
Для розгортання копії цього проєкту на Netlify необхідно прописати 5 змінних:

### Блок GitHub (Для роботи Адмінки)
| Key | Value | Опис |
|---|---|---|
| `GITHUB_TOKEN` | `ghp_...` | Personal Access Token (Classic) з правами `repo`. |
| `GITHUB_OWNER` | `kviatkivskyinet-bit` | Логін власника GitHub. |
| `GITHUB_REPO` | `neuro-site` | Назва репозиторію. |

### Блок WayForPay (Для Оплати)
| Key | Value | Опис |
|---|---|---|
| `WFP_LOGIN` | `...` | Логін мерчанта. |
| `WFP_SECRET` | `...` | Секретний ключ (Symmetric Key). |

---

## 3. Архітектура та Файлова структура

### 🧠 Ядро системи
* **`src/lib/courses-data.ts`** — Головний файл даних. Містить JSON-масив курсів. Адмінка перезаписує саме цей файл.
* **`next.config.js`** — Налаштування збірки. **ВАЖЛИВО:** Рядок `output: 'export'` має бути ЗАКОМЕНТОВАНИЙ (`//`), щоб працювали API-роути.

### 🛠️ Адмін-панель (`src/app/admin/page.tsx`)
* Працює на стороні клієнта.
* Має кнопку "Оновити сайт", яка відправляє JSON на `api/admin/save`.
* Має завантаження фото, яке відправляє файл на `api/admin/upload`.

### 🔌 API (Serverless Functions)
* **`src/app/api/admin/save/route.ts`**: Приймає контент, отримує SHA файлу на GitHub, робить Commit.
* **`src/app/api/admin/upload/route.ts`**: Приймає файл, конвертує в Base64, робить Commit у папку `public/images`.
* **`src/app/api/wayforpay/route.ts`**: Генерує криптографічний підпис MD5 для безпечної оплати.

---

## 4. Історія змін та рішень (Log)

### Етап 1: Відв'язка від платформи Same.new
* **Проблема:** Сайт залежав від платного API (`same-runtime`), не працювала збірка.
* **Рішення:** 1. Видалено папку `src/app/api` (стару).
    2. Видалено `same-runtime` з `package.json`.
    3. Видалено `jsxImportSource` з `tsconfig.json`.
    4. Створено нову структуру API.

### Етап 2: Оплата
* **Проблема:** Неможливо безпечно підписати платіж на фронтенді.
* **Рішення:** Створено серверний роут `api/wayforpay`, ключі винесено в Netlify Env Vars.

### Етап 3: Автоматизація Адмінки
* **Проблема:** Нетліфай не має бази даних для запису.
* **Рішення:** Реалізовано схему "GitHub as Database". Адмінка комітить зміни прямо в код через GitHub API.

---

## 5. Алгоритм клонування (Як створити такий же сайт для іншого проєкту)
1. Скопіювати цей репозиторій.
2. Очистити файл `courses-data.ts` (залишити пустий масив).
3. Залити на новий GitHub репозиторій (Private).
4. Підключити до Netlify.
5. Згенерувати новий `GITHUB_TOKEN` для нового акаунту.
6. Прописати нові змінні середовища в Netlify.
